### 数据接入与解析

将**docling** 封装为一个独立的数据预处理服务。进行版面分析，剔除页眉页脚的干扰，将复杂的 PDF 完美还原为**带有层级结构的 Markdown**，特别是要保真还原 Markdown 表格。
  
  切片策略
  
  **元数据注入**
每一个切出来的 Chunk，都必须强制绑定极其丰富的 Metadata，存入 Milvus 中
用于做自查询/关键词过滤

### 向量库设计与检索策略


**数据权限隔离**
律所有严格的保密要求。在 RAG 检索时，不能让 A 团队的律师搜到 B 团队的机密案卷。
将当前用户的 group_id 作为标量过滤条件传入，避免越权

